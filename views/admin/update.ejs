<!-- update.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Product</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: black;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;

    }
    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px;
      
      color: #f2be6f;
      border-radius: 0 0 10px 10px;
    }
    .header h1 {
      margin: 0;
      font-size: 2.5rem;
      font-weight: bold;
    }
    .container {
      margin-top: 30px;
      max-width: 600px;
      background: rgb(58, 53, 53);
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(153, 214, 171, 0.1);
    }
    .form-group {
      margin-bottom: 20px;
      color: #f2be6f;
    }
    .form-group label {
      font-weight: bold;
    }
    .form-group input[type="text"], .form-group input[type="number"], .form-group input[type="file"] {
      width: 100%;
      padding: 10px;
      font-size: 16px;
      border: 1px solid orange;
      color: rgb(173, 158, 158);
      border-radius: 20px;
      background: transparent;
    }
    button {
            background: #f2be6f;
            color: black;
            border: none;
            padding: 10px 20px;
            border-radius: 20px;
            font-weight: bold;
            cursor: pointer;
            transition: background 0.3s;
        }
        button:hover {
            background: rgb(214, 139, 54);
        }
        .container {
      margin-top: 30px;
      max-width: 600px;
      background: rgba(255, 255, 255, 0.2); /* Semi-transparent background */
      backdrop-filter: blur(10px); /* Blur effect */
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(153, 214, 171, 0.1);
    }
  </style>
</head>
<body>
 

<div class="header">
    <h1>Edit Product</h1>
    <a href="/products" class="btn btn-secondary" style="background-color: #f2be6f; color: black;">Back to Products</a>
</div>
<div class="container">
    <form action="/products/<%= product._id %>/update" method="POST" enctype="multipart/form-data">
        <div class="form-group">
            <label for="dishName">Product Name:</label>
            <input type="text" id="dishName" name="dishName" value="<%= product.dishName %>" required>
        </div>
        <div class="form-group">
            <label for="price">Price ($):</label>
            <input type="number" id="price" name="price" min="0" step="0.01" value="<%= product.price %>" required>
        </div>
        <div class="form-group">
            <label for="quantity">Quantity:</label>
            <input type="number" id="quantity" name="quantity" min="0" value="<%= product.quantity %>" required>
        </div>
        <div class="form-group">
            <label for="image">Upload New Image:</label>
            <input type="file" id="image" name="image" accept=".jpg, .jpeg, .png">
        </div>
        <button type="submit" style="background-color: rgb(214, 139, 54);">Update Product</button>
    </form>
</div>

</body>
</html>
