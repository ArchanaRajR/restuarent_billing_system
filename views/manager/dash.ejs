<!-- <!DOCTYPE html>
<html lang="en">
<head>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: black;
      margin: 0;
      padding: 0;
    }

    h1 {
      background-color: #531095;
      color: #fff;
      padding: 20px;
      margin: 0;
    }

    h2 {
      color: #fff;
      font-size: 24px;
      margin-top: 20px;
      text-align: center; 
    }
h3{
  color: #fff;
}
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
      
    }

    th, td {
      padding: 12px 15px;
      text-align: left;       
    }

    th {
      background: linear-gradient(to right, #ED8F03, #FFB75E); 
      color:black;
      width: 800px;
    }

    tr:nth-child(even) {
        background-color: #757d877d;
    color: #f2be6f;
    }
    tr{
      background-color: #8fa1b47d;
      color: #f2be6f;
    }

    

    td a {
      display: inline-block;
      text-decoration: none;
      padding: 5px 10px;
      background-color:red;
      color: #fff;
      border-radius: 5px;
      transition: background-color 0.3s;
    }

    td a:hover {
      background-color: #0056b3;
    }

    .create-button {
      display: inline-block;
      text-decoration: none;
      padding: 10px 20px;
      background-color: #28a745; /* Green color for the button */
      color: #fff;
      border-radius: 8px;
      transition: background-color 0.3s;
      margin-top: 7px;
      margin-left: 5px; /* Add some spacing between the button and the table */
    }

    .create-button:hover {
      background-color: #218838; /* Darker green color on hover */
    }
    .search-container {
      text-align: center;
      margin-top: 20px;
    }

    .search-input {
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 5px;
      width: 300px;
      margin-right: 10px;
    }

    .search-button {
      padding: 10px 20px;
      background-color: #007bff;
      color: #fff;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s;
    }

    
    .search-button:hover {
      background-color: #751b1b;
    }
    #logout{
      background-color:rgb(212, 151, 38);
    }
  </style>
</head>
<body>
 
  
  <a href="/manager/managerFront" id="logout" class="create-button">Back</a>
  
 

  <h2>Manager List</h2>
  <div class="search-container">
    <form action="/manager/managerDash" method="get">
      <input type="search" name="search" id="search-input" class="search-input" placeholder="Search..." value="<%= search %>">
    </form>
</div>

<h3>Search Results for: <%= search %></h3>

<table>
    <thead>   
        <tr>
          <th>ID</th>
          <th>Username</th>
          <th>password</th>
          <th>Email</th>
          <th>Delete</th>
          <th>Edit</th>
        </tr>
    </thead>
    <tbody>
      <% user.forEach(user => { %>
        <tr>
          <td><%= user.id %></td>
          <td><%= user.name %></td>
          <td><%= user.password %></td>
          <td><%= user.email %></td>
          <td><a href="/manager/delete/<%= user.id%>" class="delete-link" onclick="return confirmDelete();">Delete</a></td>
          <td><a href="/manager/edit/<%= user.id %>">Edit</a></td> 
      <% }) %>
    </tbody>
</table>

<script>
    function confirmDelete() {
        return confirm("Are you sure you want to delete this user?");
    }
</script>
  
  </script>
</body>
</html>
     -->
     <!DOCTYPE html>
     <html lang="en">
     <head>
       <style>
         body {
           font-family: Arial, sans-serif;
           background: black;
           margin: 0;
           padding: 0;
         }
     
         h1 {
           background-color: #531095;
           color: #fff;
           padding: 20px;
           margin: 0;
         }
     
         h2 {
           color: #fff;
           font-size: 24px;
           margin-top: 20px;
           text-align: center; 
         }
     
         h3 {
           color: #fff;
         }
     
         table {
           width: 100%;
           border-collapse: separate;
           border-spacing: 0 1rem; /* Add spacing between rows */
           margin-top: 20px;
         }
     
         th, td {
           padding: 12px 15px;
           text-align: left;       
         }
     
         th {
           background: linear-gradient(to right, #ED8F03, #FFB75E); 
           color:black;
         }
     
         tr {
           background: rgba(255, 255, 255, 0.1); /* Semi-transparent white background */
           backdrop-filter: blur(10px); /* Blur effect */
           border-radius: 10px; /* Rounded corners */
           color: #fff; /* Text color */
           box-shadow: 0 4px 6px rgba(122, 110, 110, 0.1);
           overflow: hidden; /* Ensure rounded corners for children */
           margin-bottom: 1rem;
         }
     
         tr:nth-child(even) {
           background: rgba(252, 235, 235, 0.1); /* Same semi-transparent white background for even rows */
         }
     
         td a {
           display: inline-block;
           text-decoration: none;
           padding: 5px 10px;
           background-color: #f2be6f; /* Match the back button color */
           color:black;
           border-radius: 5px;
           transition: background-color 0.3s;
         }
     
         td a:hover {
           background-color: #c29a35;
         }
     
         .create-button {
           display: inline-block;
           text-decoration: none;
           padding: 10px 20px;
           background-color: #28a745; /* Green color for the button */
           color: #fff;
           border-radius: 8px;
           transition: background-color 0.3s;
           margin-top: 7px;
           margin-left: 5px; /* Add some spacing between the button and the table */
         }
     
         .create-button:hover {
           background-color: #218838; /* Darker green color on hover */
         }
     
         .search-container {
           text-align: center;
           margin-top: 20px;
         }
     
         .search-input {
           padding: 10px;
           border: 1px solid #ccc;
           border-radius: 5px;
           width: 300px;
           margin-right: 10px;
         }
     
         .search-button {
           padding: 10px 20px;
           background-color: #007bff;
           color: #fff;
           border: none;
           border-radius: 5px;
           cursor: pointer;
           transition: background-color 0.3s;
         }
     
         .search-button:hover {
           background-color: #751b1b;
         }
     
         #logout {
           background-color: #f2be6f;
           color: black;
         }
         .search-input {
            padding: 10px 10px 10px 35px; /* Add padding to the left to make room for the icon */
            border: 2.5px solid #f2be6f; /* Orange border */
            border-radius: 12px;
            width: 300px;
            margin-right: 10px;
        }
       </style>
     </head>
     <body>
       <a href="/manager/managerFront" id="logout" class="create-button">Back</a>
     
       <h2>Manager List</h2>
       <div class="search-container">
         <form action="/manager/managerDash" method="get">
           <input type="search" name="search" id="search-input" class="search-input" placeholder="Search..." value="<%= search %>">
         </form>
       </div>
     
       <h3>Search Results for: <%= search %></h3>
     
       <table>
         <thead>   
           <tr>
             <th>ID</th>
             <th>Username</th>
             <th>Password</th>
             <th>Email</th>
             <th>Delete</th>
             <th>Edit</th>
           </tr>
         </thead>
         <tbody>
           <% user.forEach((user,index) => { %>
             <tr>
               <td><%= index+1 %></td>
               <td><%= user.name %></td>
               <td><%= user.password %></td>
               <td><%= user.email %></td>
               <td><a href="/manager/delete/<%= user.id %>" class="delete-link" onclick="return confirmDelete();">Delete</a></td>
               <td><a href="/manager/edit/<%= user.id %>">Edit</a></td> 
             </tr>
           <% }) %>
         </tbody>
       </table>
     
       <script>
         function confirmDelete() {
           return confirm("Are you sure you want to delete this user?");
         }
       </script>
     </body>
     </html>
     